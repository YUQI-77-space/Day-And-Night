# DayAndNight 游戏开发编码规范

> **项目代号：** DayAndNight  
> **文档版本：** 1.0.0  
> **适用范围：** 所有项目开发人员  
> **最后更新：** 2026-02-03

---

## 目录

1. [概述](#1-概述)
2. [命名规范](#2-命名规范)
3. [代码结构规范](#3-代码结构规范)
4. [注释规范](#4-注释规范)
5. [代码格式规范](#5-代码格式规范)
6. [错误处理规范](#6-错误处理规范)
7. [性能优化规范](#7-性能优化规范)
8. [Unity特定规范](#8-unity特定规范)
9. [设计模式应用](#9-设计模式应用)
10. [Git工作流规范](#10-git工作流规范)
11. [代码审查规范](#11-代码审查规范)
12. [测试规范](#12-测试规范)
13. [文档规范](#13-文档规范)
14. [最佳实践](#14-最佳实践)
15. [常见问题解答](#15-常见问题解答)
16. [附录](#16-附录)

---

## 1. 概述

### 1.1 文档目的

本文档旨在为DayAndNight项目建立统一、标准化、可执行的编码规范。良好的编码规范是大型游戏项目成功的关键因素之一，它能够：

- **提高代码可读性**：统一的代码风格使所有团队成员能够快速理解他人的代码
- **降低维护成本**：规范化的代码更容易维护、调试和扩展
- **促进团队协作**：明确的规范减少代码审查时的争议，提高协作效率
- **保证代码质量**：规范化的流程确保代码质量的一致性
- **便于知识传承**：新成员能够快速上手，理解项目架构

### 1.2 适用范围

本文档适用于DayAndNight项目的所有开发人员，包括：

- 核心系统开发
- 游戏玩法开发
- UI系统开发
- 工具脚本开发
- 编辑器扩展开发
- 测试代码开发

### 1.3 规范级别定义

本文档中的规范分为三个级别：

| 级别 | 符号 | 说明 |
|-----|------|------|
| **强制** | 🔴 | 必须遵守，违反将导致代码审查不通过 |
| **推荐** | 🟡 | 强烈建议遵守，违反需要有合理理由 |
| **参考** | 🟢 | 建议遵守，根据具体情况灵活处理 |

### 1.4 规范维护

- 本文档由项目技术负责人维护
- 任何规范修改需要经过团队讨论和批准
- 重大规范变更需要更新文档版本号
- 建议每季度进行一次规范回顾和更新

---

## 2. 命名规范

命名是代码可读性的基础，良好的命名能够使代码自文档化，减少注释的需求。

### 2.1 命名法约定

#### 2.1.1 PascalCase（大驼峰命名法）

**规则：** 每个单词的首字母大写，其他字母小写

**适用场景：** 
- 类名
- 接口名
- 方法名
- 命名空间名
- 公共字段
- 枚举值

```csharp
// 正确示例
public class GameManager { }
public interface IPlayerController { }
public void InitializeGame() { }
public enum ItemType { HealthPotion, ManaPotion, StrengthBuff }

// 错误示例
public class gameManager { }
public class GAMEMANAGER { }
public class game_Manager { }
```

#### 2.1.2 camelCase（小驼峰命名法）

**规则：** 第一个单词的首字母小写，后续单词的首字母大写

**适用场景：**
- 局部变量
- 方法参数
- 私有字段（不使用下划线前缀时）

```csharp
// 正确示例
public int playerHealth;
public float movementSpeed;
public void CalculateDamage(int baseDamage, float criticalMultiplier)

// 错误示例
public int PlayerHealth;  // 公共字段不应使用camelCase
public float MOVEMENTSPEED;  // 使用UPPER_CASE更合适
public int damage;  // 太简单，缺乏描述性
```

#### 2.1.3 UPPER_CASE（下划线大写法）

**规则：** 所有字母大写，单词之间用下划线分隔

**适用场景：**
- 常量（const）
- 静态只读字段

```csharp
// 正确示例
public const int MAX_HEALTH = 100;
public const float GRAVITY = -9.81f;
private static readonly string CONFIG_PATH = "config.json";

// 错误示例
public const int MaxHealth = 100;  // 常量应使用UPPER_CASE
public static readonly string configPath = "config.json";
```

#### 2.1.4 下划线前缀命名法

**规则：** 私有字段使用下划线前缀，后跟camelCase

**适用场景：**
- 类的私有字段

```csharp
// 正确示例
private int _currentHealth;
private float _movementSpeed;
private bool _isInitialized;
private GameObject _playerObject;

// 错误示例
private int currentHealth;  // 缺乏下划线前缀
private int m_currentHealth;  // 旧的Unity命名风格，不再推荐
private int _CurrentHealth;  // 混合了camelCase和下划线前缀
```

### 2.2 类命名规范 🔴

#### 2.2.1 基础类命名

**规则：** 使用PascalCase，确保名称清晰描述类的职责

| 后缀 | 用途 | 示例 |
|-----|------|------|
| **Manager** | 管理器类，负责协调和管理特定系统 | `GameManager`, `AudioManager`, `UIManager` |
| **Controller** | 控制器类，控制特定对象的行为 | `PlayerController`, `CameraController` |
| **Handler** | 处理器类，处理特定事件或请求 | `EventHandler`, `InputHandler` |
| **System** | 系统类，表示一个完整的子系统 | `CombatSystem`, `InventorySystem` |
| **Factory** | 工厂类，负责创建对象 | `ItemFactory`, `EnemyFactory` |
| **Builder** | 构建器类，复杂对象的构建 | `CharacterBuilder`, `UIBuilder` |
| **Repository** | 仓储类，数据的存取 | `PlayerRepository`, `ItemRepository` |
| **Service** | 服务类，提供特定服务 | `NetworkService`, `AnalyticsService` |
| **Provider** | 提供者类，提供数据或资源 | `ConfigurationProvider`, `AssetProvider` |
| **Helper** | 工具类，提供辅助方法 | `MathHelper`, `StringHelper` |
| **Util** | 工具类（与Helper二选一） | `GameUtil`, `DateUtil` |

```csharp
// 正确示例
public class GameManager : MonoBehaviour { }
public class PlayerController : MonoBehaviour { }
public class InventorySystem : MonoBehaviour { }
public class AudioManager : MonoBehaviour { }
public class ItemFactory : MonoBehaviour { }

// 错误示例
public class Game { }  // 过于笼统
public class GM { }  // 使用缩写，不清晰
public class GameControl { }  - 不一致的命名风格
```

#### 2.2.2 继承类命名

**规则：** 基础类使用「Base」前缀，接口使用「I」前缀

| 前缀/后缀 | 用途 | 示例 |
|----------|------|------|
| **Base** | 基类，表示可以被继承的抽象类 | `BaseManager`, `BaseEnemy`, `BaseItem` |
| **I** | 接口，表示契约或能力 | `IManager`, `ISaveable`, `IDamageable` |
| **Abstract** | 抽象类 | `AbstractFactory`, `AbstractBuilder` |

```csharp
// 正确示例
public abstract class BaseManager : MonoBehaviour { }
public interface IManager { }
public abstract class BaseEnemy : MonoBehaviour { }
public interface IDamageable { }

// 错误示例
public abstract class ManagerBase { }  // 应使用Base前缀
public interface ManagerInterface { }  // 应使用I前缀
public class EnemyBase : MonoBehaviour { }  // 应使用Base前缀
```

#### 2.2.3 组件类命名

**规则：** MonoBehaviour脚本使用描述性名称，通常包含功能关键词

```csharp
// 正确示例
public class PlayerMovement : MonoBehaviour { }
public class EnemyAI : MonoBehaviour { }
public class HealthBar : MonoBehaviour { }
public class WeaponInventory : MonoBehaviour { }

// 错误示例
public class Player : MonoBehaviour { }  // 过于笼统
public class Script1 : MonoBehaviour { }  // 缺乏描述性
public class PlayerStuff : MonoBehaviour { }  - 不专业
```

### 2.3 方法命名规范 🔴

#### 2.3.1 公共方法

**规则：** 使用PascalCase，方法名应清晰表达方法的功能，使用动词开头

**方法命名模式：**

| 模式 | 说明 | 示例 |
|-----|------|------|
| `Verb+Noun` | 动词+名词，描述操作对象 | `LoadGame()`, `SaveGame()`, `CreatePlayer()` |
| `Verb+Adjective` | 动词+形容词，描述状态变化 | `EnableDebug()`, `DisableAudio()` |
| `Is/Has/Can+...` | 布尔判断方法 | `IsAlive()`, `HasPermission()`, `CanMove()` |
| `Get+...` | 获取值方法 | `GetHealth()`, `GetComponent()` |
| `Set+...` | 设置值方法 | `SetHealth()`, `SetPosition()` |
| `Try+...` | 尝试操作方法 | `TryConnect()`, `TryParse()` |

```csharp
// 正确示例
public void InitializeGame() { }
public bool LoadPlayerData(string savePath) { }
public IEnumerator LoadSceneAsync(string sceneName) { }
public bool IsPlayerAlive() { }
public bool HasEnoughGold(int amount) { }
public GameObject InstantiatePlayer() { }
public void SetVolume(float volume) { }
public bool TryGetComponent<T>(out T component) { }

// 错误示例
public void StartGame() { }  // 可以，但更具体更好
public void Do() { }  // 缺乏描述性
public void GameInit() { }  - 应使用Verb+Noun模式
public bool Get() { }  - 缺乏获取什么的信息
```

#### 2.3.2 私有方法

**规则：** 使用camelCase或下划线前缀+camelCase（团队约定）

```csharp
// 方案A：使用下划线前缀（推荐）
private void _InitializeManagers() { }
private bool _LoadDataFromFile(string path) { }
private void _UpdatePlayerState() { }

// 方案B：使用标准camelCase
private void InitializeManagers() { }
private bool LoadDataFromFile(string path) { }
private void UpdatePlayerState() { }

// 错误示例
private void INIT() { }  // 全部大写，不规范
private void doSomething() { }  // 使用小写开头
```

#### 2.3.3 异步方法

**规则：** 异步方法使用Async后缀，返回Task或IEnumerator

```csharp
// 正确示例
public async Task LoadSceneAsync(string sceneName) { }
public IEnumerator LoadSceneCoroutine(string sceneName) { }
public async Task<PlayerData> FetchPlayerDataAsync(int playerId) { }

// 错误示例
public Task LoadScene(string sceneName) { }  // 缺少Async后缀
public IEnumerator LoadScene(string sceneName) { }  // 缺少Coroutine后缀
```

### 2.4 变量命名规范 🔴

#### 2.4.1 字段命名

**规则：**
- 公共字段：使用PascalCase
- 私有字段：使用下划线前缀+camelCase
- 保护字段：使用下划线前缀+camelCase（与私有字段一致）

```csharp
// 正确示例
public int MaxHealth;  // 公共字段
public float MovementSpeed;  // 公共字段
private int _currentHealth;  // 私有字段
private float _movementSpeed;  // 私有字段
protected bool _isInitialized;  // 保护字段
protected GameObject _playerObject;  // 保护字段

// 错误示例
public int maxHealth;  // 公共字段不应使用camelCase
private int CurrentHealth;  // 私有字段缺少下划线前缀
private int m_currentHealth;  // 旧的Unity命名风格
```

#### 2.4.2 常量命名

**规则：** 使用UPPER_CASE，单词之间用下划线分隔

```csharp
// 正确示例
public const int MAX_HEALTH = 100;
public const float GRAVITY = -9.81f;
public const string GAME_VERSION = "1.0.0";
private const int DEFAULT_PORT = 8080;

// 错误示例
public const int MaxHealth = 100;  // 应使用UPPER_CASE
public const float gravity = -9.81f;  // 应使用UPPER_CASE
public const string gameVersion = "1.0.0";  // 应使用UPPER_CASE
```

#### 2.4.3 静态变量命名

**规则：**
- 静态字段：使用下划线前缀+UPPER_CASE（常量）或下划线前缀+camelCase（非常量）
- 静态属性：使用PascalCase

```csharp
// 正确示例
public static int InstanceCount { get; private set; }
public static GameManager Instance { get; private set; }
private static readonly int _maxInstances = 10;
public static readonly Vector2 _zeroVector = Vector2.zero;

// 错误示例
public static int instanceCount { get; private set; }  // 应使用PascalCase
public static GameManager instance { get; private set; }  // 应使用PascalCase
```

#### 2.4.4 参数命名

**规则：** 使用camelCase，确保名称清晰描述参数的用途

```csharp
// 正确示例
public void SetHealth(int newHealth, float duration) { }
public bool HasItem(string itemId, int quantity) { }
public void MovePlayer(Vector3 targetPosition, float speed) { }
public T GetComponent<T>(bool includeInactive = false) where T : Component { }

// 错误示例
public void SetHealth(int h, float t) { }  // 使用缩写，不清晰
public void SetHealth(int health, float time) { }  // 但time不如duration具体
public void MovePlayer(Vector3 pos, float spd) { }  - 使用缩写
```

#### 2.4.5 局部变量命名

**规则：** 使用camelCase，变量名应反映变量的用途

```csharp
// 正确示例
int currentHealth = player.Health;
float movementSpeed = 5.0f;
bool isAlive = true;
var enemies = GameObject.FindGameObjectsWithTag("Enemy");
var filteredItems = items.Where(i => i.IsActive);

// 错误示例
int a = player.Health;  // 缺乏描述性
int temp = CalculateDamage();  // 应使用更具体的名称
var list = new List<Item>();  - 应使用更具体的名称
```

### 2.5 命名空间命名规范 🔴

#### 2.5.1 命名空间结构

**规则：** 使用公司/团队名.游戏模块.功能模块的结构

```csharp
// 正确示例
namespace DayAndNight.Core
{
    namespace Managers { }
    namespace Utilities { }
}

namespace DayAndNight.Gameplay
{
    namespace Combat { }
    namespace Attributes { }
    namespace Quests { }
}

namespace DayAndNight.UI
{
    namespace HUD { }
    namespace Menus { }
    namespace Dialogs { }
}

namespace DayAndNight.Data
{
    namespace Config { }
    namespace Database { }
    namespace Serialization { }
}

// 扁平化命名空间（推荐）
namespace DayAndNight.Core.Managers
{
    public class GameManager { }
    public class TimeManager { }
}

namespace DayAndNight.Gameplay.Combat
{
    public class CombatSystem { }
    public class DamageCalculator { }
}

namespace DayAndNight.UI.HUD
{
    public class HealthBar { }
    public class InventoryPanel { }
}
```

#### 2.5.2 命名空间命名规则

**规则：**
- 使用PascalCase
- 避免缩写
- 与目录结构对应

```csharp
// 正确示例
namespace DayAndNight.Core { }
namespace DayAndNight.Gameplay.Attributes { }
namespace DayAndNight.Audio { }

// 错误示例
namespace DayAndNight.Core.Managers { }  // Managers是冗余的
namespace DN { }  - 使用缩写
namespace Core { }  - 缺少项目前缀
```

### 2.6 文件命名规范 🔴

#### 2.6.1 脚本文件命名

**规则：** 文件名必须与类名完全一致

```
Assets/_Project/Scripts/Core/GameManager.cs
Assets/_Project/Scripts/Core/GameManagerEditor.cs

Assets/_Project/Scripts/Gameplay/Player/PlayerController.cs
Assets/_Project/Scripts/Gameplay/Combat/CombatSystem.cs
```

#### 2.6.2 资源文件命名

**规则：** 使用描述性名称，保持一致性

| 资源类型 | 命名模式 | 示例 |
|---------|---------|------|
| 预制体 | `Type_Name_Postfix` | `Prefab_Player_Hero.prefab`, `Prefab_Enemy_Slime.prefab` |
| 场景 | `Scene_Name` | `Scene_Main_Town.unity`, `Scene_Combat_Boss.unity` |
| 精灵图集 | `Atlas_Name` | `Atlas_Characters_Heroes.spriteatlas`, `Atlas_Items_Consumables.spriteatlas` |
| 材质 | `Mat_Name` | `Mat_Player_Hero.mat`, `Mat_Environment_Grass.mat` |
| 动画控制器 | `AC_Name` | `AC_Player_Hero.controller`, `AC_Enemy_Slime.controller` |
| 动画片段 | `Anim_Name_Action` | `Anim_Player_Idle.anim`, `Anim_Player_Run.anim` |

### 2.7 特殊命名规范 🟡

#### 2.7.1 事件命名

**规则：** 事件使用过去式或形容词，表示"发生了某事"

```csharp
// 正确示例
public event Action OnGameStarted;
public event Action OnGamePaused;
public event Action<int> OnHealthChanged;
public event Action<Player, DamageInfo> OnPlayerDamaged;

// 错误示例
public event Action StartGame;  // 应该是"开始游戏"这个事件
public event Action GameStart;  - 缺乏动词
```

#### 2.7.2 委托命名

**规则：** 委托类型使用Delegate后缀，参数命名反映委托的用途

```csharp
// 正确示例
public delegate void GameActionDelegate();
public delegate void DamageDelegate(int damage, DamageType type);
public delegate bool ValidationDelegate<T>(T value);

// 错误示例
public delegate void Action();  // 与System.Action冲突
public delegate void DoSomething();  - 不清晰
```

#### 2.7.3 枚举命名

**规则：** 枚举类型使用PascalCase，枚举值使用UPPER_CASE

```csharp
// 正确示例
public enum ItemType
{
    HealthPotion,
    ManaPotion,
    StrengthBuff,
    SpeedBuff
}

public enum GameState
{
    MainMenu,
    Playing,
    Paused,
    GameOver
}

// 错误示例
public enum item_type {  // 应使用PascalCase
    health_potion,  // 应使用UPPER_CASE
}

public enum State  // 缺少前缀
{
    MAIN_MENU,
    PLAYING,
}
```

---

## 3. 代码结构规范

### 3.1 文件结构 🔴

#### 3.1.1 单文件单类原则

**规则：** 每个C#文件应该只包含一个公共类

```csharp
// GameManager.cs
namespace DayAndNight.Core.Managers
{
    public class GameManager : MonoBehaviour
    {
        // GameManager的实现
    }
}

// 错误示例：一个文件包含多个类
public class GameManager : MonoBehaviour { }
public class GameManagerData { }  - 应放在单独的文件中
```

#### 3.1.2 文件组织顺序

**规则：** 文件中的代码应按特定顺序组织

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;

namespace DayAndNight.Core.Managers
{
    /// <summary>
    /// 游戏主管理器类
    /// </summary>
    public class GameManager : MonoBehaviour
    {
        #region 常量
        public const int MAX_PLAYERS = 4;
        #endregion

        #region 静态字段
        public static GameManager Instance { get; private set; }
        #endregion

        #region 公共字段
        public int PlayerCount;
        #endregion

        #region 序列化字段
        [SerializeField] private int _maxPlayers = 4;
        [SerializeField] private bool _isDebugMode = false;
        #endregion

        #region 私有字段
        private bool _isInitialized;
        private List<Player> _players;
        #endregion

        #region 事件
        public event Action OnGameInitialized;
        public event Action OnGameStarted;
        #endregion

        #region 属性
        public bool IsInitialized => _isInitialized;
        public int ActivePlayerCount => _players.Count;
        #endregion

        #region 生命周期方法
        private void Awake()
        {
            // 单例初始化
            if (Instance == null)
            {
                Instance = this;
                DontDestroyOnLoad(gameObject);
            }
            else
            {
                Destroy(gameObject);
                return;
            }
        }

        private void Start()
        {
            Initialize();
        }

        private void Update()
        {
            // 游戏循环更新
        }
        #endregion

        #region 公共方法
        public void Initialize()
        {
            // 初始化逻辑
        }

        public void StartGame()
        {
            // 开始游戏
        }

        public void PauseGame()
        {
            // 暂停游戏
        }
        #endregion

        #region 私有方法
        private void _LoadGameData()
        {
            // 加载游戏数据
        }

        private void _InitializePlayers()
        {
            // 初始化玩家
        }
        #endregion

        #region 协程方法
        private IEnumerator _LoadSceneRoutine(string sceneName)
        {
            // 场景加载协程
            yield return null;
        }
        #endregion

        #region 静态方法
        public static GameManager Create()
        {
            // 静态工厂方法
            return new GameObject("GameManager").AddComponent<GameManager>();
        }
        #endregion

        #region 嵌套类
        [Serializable]
        public class GameSettings
        {
            public int MaxPlayers;
            public float GameSpeed;
        }
        #endregion
    }
}
```

### 3.2 类结构规范 🔴

#### 3.2.1 Region使用规范

**规则：** 使用#region组织代码块，提高可读性

**推荐的Region顺序：**
1. 常量（Constants）
2. 静态字段（Static Fields）
3. 公共字段（Public Fields）
4. 序列化字段（SerializeField Fields）
5. 私有字段（Private Fields）
6. 事件（Events）
7. 属性（Properties）
8. 生命周期方法（Unity Messages）
9. 公共方法（Public Methods）
10. 私有方法（Private Methods）
11. 协程方法（Coroutines）
12. 静态方法（Static Methods）
13. 嵌套类（Nested Classes）

#### 3.2.2 类大小控制

**规则：** 
- 单一职责原则：每个类应该只有一个改变的理由
- 推荐类长度：200-500行
- 超过500行应考虑拆分
- 核心管理器类可以适当放宽，但不应超过1000行

```csharp
// 正确示例：职责单一的类
public class HealthSystem : MonoBehaviour
{
    // 专注于生命值管理
    [SerializeField] private int _maxHealth = 100;
    [SerializeField] private int _currentHealth;
    
    public event Action<int, int> OnHealthChanged;  // healthChanged;
    public event Action OnDied;
    
    public void TakeDamage(int damage) { }
    public void Heal(int amount) { }
    public bool IsAlive => _currentHealth > 0;
}

// 错误示例：职责过多的类
public class Player : MonoBehaviour
{
    // 这个类做了太多事情，应该拆分
    private void Update() { }  // 移动逻辑
    private void Update() { }  // 战斗逻辑
    private void Update() { }  // UI逻辑
    private void Update() { }  // 存档逻辑
}
```

### 3.3 方法结构规范 🟡

#### 3.3.1 方法长度控制

**规则：** 
- 推荐方法长度：10-50行
- 超过50行应考虑拆分成多个方法
- 协程方法可以适当放宽

```csharp
// 正确示例：简短的方法
public void Initialize()
{
    _LoadConfiguration();
    _InitializeManagers();
    _SetupEventListeners();
    _LoadSavedData();
}

private void _LoadConfiguration()
{
    var config = ConfigurationManager.Load();
    _maxPlayers = config.MaxPlayers;
    _gameSpeed = config.DefaultSpeed;
}

private void _InitializeManagers()
{
    GameManager.Instance.Initialize();
    AudioManager.Instance.Initialize();
    UIManager.Instance.Initialize();
}

// 错误示例：过长方法
public void ProcessPlayerInput()
{
    // 100+行的输入处理逻辑，应该拆分
}
```

#### 3.3.2 参数数量控制

**规则：** 
- 推荐参数数量：≤3个
- 超过3个参数应考虑使用参数对象或配置类
- 使用可选参数减少重载数量

```csharp
// 正确示例：参数数量合理
public void MovePlayer(Vector3 targetPosition) { }
public void MovePlayer(Vector3 targetPosition, float duration) { }

// 使用参数对象
public void SpawnEnemy(EnemySpawnConfig config)
{
    var enemy = Instantiate(config.Prefab, config.Position, config.Rotation);
    enemy.Initialize(config.Level, config.Team);
}

// 错误示例：参数过多
public void SpawnEnemy(GameObject prefab, Vector3 position, Quaternion rotation, 
    int level, int team, float healthMultiplier, float speedMultiplier, 
    string aiType, bool isBoss) { }
```

### 3.4 语句规范 🟡

#### 3.4.1 简单语句

**规则：** 每行只写一条语句

```csharp
// 正确示例
playerHealth -= damage;
isDead = true;
PlaySound(hitSound);

// 错误示例
playerHealth -= damage; isDead = true;  // 一行多条语句
```

#### 3.4.2 复合语句

**规则：** 复合语句（if、while、for等）即使只有一条语句，也必须使用大括号

```csharp
// 正确示例
if (isAlive)
{
    TakeDamage(10);
}

for (int i = 0; i < 10; i++)
{
    Instantiate(enemyPrefab);
}

// 错误示例
if (isAlive)
    TakeDamage(10);  // 缺少大括号

for (int i = 0; i < 10; i++)
    Instantiate(enemyPrefab);  // 缺少大括号
```

#### 3.4.3 空语句

**规则：** 空语句应有注释说明

```csharp
// 正确示例
while (stream.Position < stream.Length)
{
    // 等待数据可用
    ;  // 空操作
}

public void EmptyMethod()
{
    // TODO: 待实现
}
```

---

## 4. 注释规范

### 4.1 XML文档注释 🔴

#### 4.1.1 必需注释的位置

**规则：** 所有公共类、公共方法、公共属性必须添加XML文档注释

```csharp
/// <summary>
/// 游戏主管理器，负责协调所有子系统和游戏生命周期管理
/// </summary>
public class GameManager : MonoBehaviour
{
    /// <summary>
    /// 当前游戏的唯一标识符
    /// </summary>
    public string GameId { get; private set; }

    /// <summary>
    /// 初始化所有管理器系统
    /// </summary>
    /// <returns>初始化是否成功</returns>
    public bool Initialize()
    {
        // 实现
    }
    
    /// <summary>
    /// 暂停或恢复游戏
    /// </summary>
    /// <param name="pause">是否暂停</param>
    public void SetPause(bool pause)
    {
        // 实现
    }
    
    /// <summary>
    /// 加载指定场景
    /// </summary>
    /// <param name="sceneName">场景名称</param>
    /// <param name="showLoadingScreen">是否显示加载界面</param>
    /// <returns>异步加载操作</returns>
    public AsyncOperation LoadScene(string sceneName, bool showLoadingScreen = true)
    {
        // 实现
    }
}
```

#### 4.1.2 常用XML标签

```csharp
/// <summary>
/// 类或方法的简要描述（必需）
/// </summary>

/// <summary>
/// 方法参数的说明（每个参数一个）
/// </summary>
/// <param name="paramName">参数描述</param>

/// <summary>
/// 方法返回值的说明
/// </summary>
/// <returns>返回值描述</returns>

/// <summary>
/// 方法可能抛出的异常说明
/// </summary>
/// <exception cref="ArgumentException">参数无效时抛出</exception>
/// <exception cref="InvalidOperationException">对象状态不正确时抛出</summary>

/// <summary>
/// 方法或类的示例代码
/// </summary>
/// <code>
/// // 使用示例
/// GameManager.Instance.Initialize();
/// </code>

/// <summary>
/// 方法或类的备注说明
/// </summary>
/// <remarks>
/// 这里可以写更详细的说明
/// </remarks>

/// <summary>
/// 引用其他成员
/// </summary>
/// <see cref="OtherClass"/>

/// <summary>
/// 允许链接到外部文档
/// </summary>
/// <a href="https://docs.unity3d.com/">Unity文档</a>
```

#### 4.1.3 复杂方法的文档注释

```csharp
/// <summary>
/// 计算最终伤害值，考虑防御、暴击和伤害减免
/// </summary>
/// <param name="attack">基础攻击力</param>
/// <param name="defense">目标防御力</param>
/// <param name="isCritical">是否暴击</param>
/// <returns>最终计算后的伤害值</returns>
/// <remarks>
/// 计算公式：damage = (baseDamage - defense) * criticalMultiplier * damageReduction
/// 最小伤害为1，最大伤害无上限
/// </remarks>
public int CalculateDamage(int attack, int defense, bool isCritical = false)
{
    // 计算逻辑
}
```

### 4.2 代码内注释 🟡

#### 4.2.1 注释原则

**规则：**
- 解释"为什么"，而不是"是什么"
- 复杂逻辑必须添加注释
- 注释应保持更新，与代码同步
- 使用中文注释（团队约定）

```csharp
// 正确示例
// 确保最小伤害为1，避免无敌情况
baseDamage = Mathf.Max(1, baseDamage);

// 暴击判定（15%基础暴击率 + 装备加成）
bool isCritical = Random.value <= (0.15f + CriticalChanceBonus);

// 使用DoTween进行平滑移动，避免直接修改位置
DOTween.To(() => transform.position, x => transform.position = x, targetPosition, duration);

// 错误示例
// 设置baseDamage为1
baseDamage = Mathf.Max(1, baseDamage);  // 注释重复了代码

// 计算伤害
bool isCritical = Random.value <= 0.15f;  - 没有说明0.15f的含义
```

#### 4.2.2 注释位置

**规则：** 注释应在需要说明的代码上方或同行

```csharp
// 正确示例：上方注释
// 计算最终伤害，考虑防御减免
int finalDamage = CalculateDamage(attack, defense);

// 正确示例：行尾注释（简短说明）
var item = Instantiate(itemPrefab, position, Quaternion.identity);  // 生成物品

// 正确示例：代码块注释
// ========================================
// 初始化玩家数据
// ========================================
playerHealth = maxHealth;
playerMana = maxMana;
playerLevel = 1;
```

#### 4.2.3 TODO注释

**规则：** 使用TODO注释标记待完成的工作，格式：`TODO: [优先级] 描述`

```csharp
// TODO: [High] 待实现敌人AI行为
// TODO: [Medium] 优化对象池的初始化逻辑
// TODO: [Low] 添加音效反馈
// TODO: 待重构为使用事件驱动模式
```

### 4.3 区域注释 🟡

**规则：** 大型代码块可以使用区域注释增强可读性

```csharp
// ==================== 玩家状态管理 ====================
private void UpdatePlayerState()
{
    // 状态更新逻辑
}

// -------------------- 生命周期事件 --------------------
private void OnEnable()
{
    // 启用时的事件订阅
}

private void OnDisable()
{
    // 禁用时的事件取消订阅
}

// *** 调试专用代码 ***（发布时删除）
#if UNITY_EDITOR || DEVELOPMENT_BUILD
private void DrawDebugInfo()
{
    // 调试信息绘制
}
#endif
```

### 4.4 注释风格 🟡

#### 4.4.1 注释格式

**规则：** 
- 注释首字母大写
- 注释使用完整句子
- 行注释使用双斜杠
- 块注释用于临时注释

```csharp
// 正确示例
// 这是一个正确格式的注释
// 这是多行注释
// 的第二行

/* 
 * 这是一个块注释
 * 用于注释多行代码
 */

/*
 这也是有效的块注释
*/

// 错误示例
// 这是一个注释
// 这也是  // 混合了行尾注释
```

---

## 5. 代码格式规范

### 5.1 缩进与空格 🔴

#### 5.1.1 缩进规则

**规则：** 使用4个空格缩进，不使用制表符

```csharp
// 正确示例
public void Example()
{
    if (condition)
    {
        DoSomething();
    }
}

// 错误示例（使用制表符或混合缩进）
public void Example()
{
	if (condition)  // 制表符
	    DoSomething();  // 混合缩进
}
```

#### 5.1.2 空格使用规则

**规则：** 在运算符、逗号、括号周围添加空格

```csharp
// 正确示例
if (a + b > c)
{
    var result = Calculate(a, b, c);
}

for (int i = 0; i < 10; i++)
{
    // 循环体
}

public void Method(int a, int b, int c)
{
    // 方法体
}

// 错误示例
if(a+b>c){  // 缺少空格
    var result=Calculate(a,b,c);  // 缺少空格
}

for(int i=0;i<10;i++){  // 缺少空格
}
```

#### 5.1.3 括号空格规则

```csharp
// 正确示例
if (condition)
{
    // 代码
}

while (condition)
{
    // 代码
}

foreach (var item in collection)
{
    // 代码
}

Method(parameter);

// 错误示例
if(condition)  // if后缺少空格
{
    // 代码
}

Method (parameter);  // 括号前不应有空格
```

### 5.2 空行规范 🟡

#### 5.2.1 方法之间

**规则：** 方法之间使用一个空行

```csharp
public void MethodOne()
{
    // 方法体
}

public void MethodTwo()
{
    // 方法体
}
```

#### 5.2.2 逻辑块之间

**规则：** 不同的逻辑块之间使用空行分隔

```csharp
public void ComplexMethod()
{
    // 初始化逻辑
    Initialize();
    
    // 处理逻辑
    ProcessData();
    
    // 清理逻辑
    Cleanup();
}
```

#### 5.2.3 Region之间

**规则：** #region和#endregion所在行前后可使用空行

```csharp
#region 公共方法

public void PublicMethod1() { }
public void PublicMethod2() { }

#endregion

#region 私有方法

private void PrivateMethod1() { }
private void PrivateMethod2() { }

#endregion
```

### 5.3 大括号位置 🔴

**规则：** 大括号使用Allman风格（左大括号单独一行）

```csharp
// 正确示例
public void Method()
{
    if (condition)
    {
        // 代码
    }
    else
    {
        // 代码
    }
}

// 错误示例（K&R风格）
public void Method() {
    if (condition) {
        // 代码
    } else {
        // 代码
    }
}
```

### 5.4 每行字符限制 🟡

**规则：** 每行不超过120个字符

```csharp
// 正确示例
var result = SomeVeryLongMethodNameThatDoesSomething(
    parameter1, 
    parameter2, 
    parameter3);

// 错误示例（行过长）
var result = SomeVeryLongMethodNameThatDoesSomething(parameter1, parameter2, parameter3, parameter4, parameter5, parameter6);
```

### 5.5 switch语句格式 🔴

```csharp
// 正确示例
switch (state)
{
    case GameState.MainMenu:
        ShowMainMenu();
        break;
        
    case GameState.Playing:
        StartGame();
        break;
        
    case GameState.Paused:
        ShowPauseMenu();
        break;
        
    default:
        ShowError($"未知状态: {state}");
        break;
}

// 使用switch表达式（C# 8.0+）
var status = state switch
{
    GameState.MainMenu => "主菜单",
    GameState.Playing => "游戏中",
    GameState.Paused => "已暂停",
    _ => "未知"
};
```

### 5.6 集合初始化格式 🟡

```csharp
// 正确示例
var numbers = new List<int>
{
    1, 2, 3, 4, 5
};

var player = new Player
{
    Name = "Hero",
    Health = 100,
    Level = 1
};

var dictionary = new Dictionary<string, int>
{
    ["One"] = 1,
    ["Two"] = 2
};
```

---

## 6. 错误处理规范

### 6.1 异常处理 🔴

#### 6.1.1 基本原则

**规则：**
- 不要捕获并忽略异常
- 不要捕获通用Exception，除非有明确理由
- 使用try-catch处理预期异常
- 记录异常信息便于调试

```csharp
// 正确示例
try
{
    var data = LoadPlayerData(savePath);
    return data;
}
catch (FileNotFoundException ex)
{
    Logger.LogError($"存档文件不存在: {savePath}", ex);
    return null;
}
catch (IOException ex)
{
    Logger.LogError($"读取存档文件失败: {savePath}", ex);
    return null;
}

// 错误示例
try
{
    // 可能出错的代码
}
catch (Exception)  // 捕获所有异常并忽略
{
    // 什么也不做
}
```

#### 6.1.2 异常类型选择

**规则：** 使用最具体的异常类型

| 异常类型 | 使用场景 |
|---------|---------|
| `ArgumentNullException` | 参数为null |
| `ArgumentException` | 参数无效 |
| `IndexOutOfRangeException` | 索引超出范围 |
| `KeyNotFoundException` | 键不存在 |
| `InvalidOperationException` | 对象状态不允许该操作 |
| `NotImplementedException` | 功能未实现（仅用于占位） |
| `UnauthorizedAccessException` | 无权限执行操作 |

```csharp
// 正确示例
public void SetHealth(int health)
{
    if (health < 0)
    {
        throw new ArgumentException("生命值不能为负数", nameof(health));
    }
    
    if (health > MaxHealth)
    {
        throw new ArgumentException($"生命值不能超过最大值{MaxHealth}", nameof(health));
    }
    
    _health = health;
}

// 错误示例
public void SetHealth(int health)
{
    if (health < 0)
    {
        throw new Exception("生命值不能为负数");  // 应使用ArgumentException
    }
}
```

#### 6.1.3 自定义异常

**规则：** 自定义异常应继承Exception，添加必要的属性

```csharp
public class GameException : Exception
{
    public string ErrorCode { get; }
    
    public GameException(string message) : base(message)
    {
        ErrorCode = "GENERAL_ERROR";
    }
    
    public GameException(string message, string errorCode) : base(message)
    {
        ErrorCode = errorCode;
    }
    
    public GameException(string message, Exception innerException) : base(message, innerException)
    {
        ErrorCode = "GENERAL_ERROR";
    }
}
```

### 6.2 返回值处理 🟡

#### 6.2.1 使用Result模式

**规则：** 对于可能失败的操作，考虑返回Result对象而不是抛出异常

```csharp
public struct Result
{
    public bool IsSuccess { get; }
    public string ErrorMessage { get; }
    
    private Result(bool isSuccess, string errorMessage)
    {
        IsSuccess = isSuccess;
        ErrorMessage = errorMessage;
    }
    
    public static Result Success() => new Result(true, null);
    public static Result Failure(string errorMessage) => new Result(false, errorMessage);
    
    // 泛型版本
    public struct Result<T>
    {
        public T Value { get; }
        public bool IsSuccess { get; }
        public string ErrorMessage { get; }
        
        private Result(T value, bool isSuccess, string errorMessage)
        {
            Value = value;
            IsSuccess = isSuccess;
            ErrorMessage = errorMessage;
        }
        
        public static Result<T> Success(T value) => new Result<T>(value, true, null);
        public static Result<T> Failure(string errorMessage) => new Result<T>(default, false, errorMessage);
    }
}

// 使用示例
public Result<PlayerData> LoadPlayerData(string savePath)
{
    if (!File.Exists(savePath))
    {
        return Result<PlayerData>.Failure($"存档文件不存在: {savePath}");
    }
    
    try
    {
        var data = JsonUtility.FromJson<PlayerData>(File.ReadAllText(savePath));
        return Result<PlayerData>.Success(data);
    }
    catch (Exception ex)
    {
        return Result<PlayerData>.Failure($"加载存档失败: {ex.Message}");
    }
}
```

#### 6.2.2 使用out参数

**规则：** 对于需要返回多个值的简单场景，使用out参数

```csharp
// 正确示例
public bool TryParseInput(string input, out int value)
{
    return int.TryParse(input, out value);
}

// 使用
if (TryParseInput(userInput, out int value))
{
    // 使用value
}
```

### 6.3 空值处理 🟡

#### 6.3.1 空值检查

**规则：** 对可能为null的参数进行显式检查

```csharp
// 正确示例
public void ProcessPlayer(Player player)
{
    if (player == null)
    {
        throw new ArgumentNullException(nameof(player));
    }
    
    // 处理逻辑
}

// 使用空条件运算符简化
public void UpdatePlayer(Player player)
{
    player?.TakeDamage(10);  // player为null时跳过
}

// 合并运算符
public Player GetPlayer() => _player ?? CreateDefaultPlayer();
```

#### 6.3.2 可空类型

**规则：** 对于可能为null的值，使用可空类型

```csharp
// 正确示例
public int? CalculateScore()
{
    if (gameEnded)
    {
        return _score;
    }
    return null;  // 游戏未结束时返回null
}

public void DisplayScore(int? score)
{
    scoreText.text = score?.ToString() ?? "游戏中";  // 显示"游戏中"而不是空
}
```

### 6.4 日志记录 🔴

#### 6.4.1 日志级别

**规则：** 使用适当的日志级别

| 级别 | 使用场景 |
|-----|---------|
| **Error** | 错误事件，需要立即关注 |
| **Warning** | 警告事件，可能存在问题 |
| **Info** | 一般信息，重要操作记录 |
| **Debug** | 调试信息，详细诊断 |

#### 6.4.2 日志格式

```csharp
// 正确示例
Logger.LogError($"加载玩家数据失败: {savePath}", ex);
Logger.LogWarning($"存档版本不兼容: {saveVersion} (当前: {CurrentVersion})");
Logger.LogInfo($"玩家 {player.Name} 进入场景: {sceneName}");

// 错误示例
Debug.Log("error");  // 使用Logger而不是Debug
Debug.LogError(ex);  // 只打印异常对象
```

---

## 7. 性能优化规范

### 7.1 内存管理 🟡

#### 7.1.1 避免不必要的分配

```csharp
// 正确示例：复用StringBuilder
private StringBuilder _stringBuilder = new StringBuilder();

public string BuildPlayerInfo(Player player)
{
    _stringBuilder.Clear();
    _stringBuilder.AppendLine($"名称: {player.Name}");
    _stringBuilder.AppendLine($"等级: {player.Level}");
    _stringBuilder.AppendLine($"生命值: {player.Health}/{player.MaxHealth}");
    return _stringBuilder.ToString();
}

// 使用结构体而不是类（小型数据）
public struct Vector2Int
{
    public int x;
    public int y;
}

// 错误示例：在循环中分配字符串
public string BuildPlayerInfo(Player player)
{
    string result = "";  // 每次循环都分配新字符串
    result += $"名称: {player.Name}\n";
    result += $"等级: {player.Level}\n";
    result += $"生命值: {player.Health}/{player.MaxHealth}\n";
    return result;
}
```

#### 7.1.2 使用对象池

```csharp
public class ObjectPool<T> where T : Component
{
    private Queue<T> _pool = new Queue<T>();
    private T _prefab;
    private Transform _container;
    
    public ObjectPool(T prefab, int initialSize, Transform container = null)
    {
        _prefab = prefab;
        _container = container;
        
        for (int i = 0; i < initialSize; i++)
        {
            var obj = Object.Instantiate(prefab, container);
            obj.gameObject.SetActive(false);
            _pool.Enqueue(obj);
        }
    }
    
    public T Get()
    {
        if (_pool.Count > 0)
        {
            var obj = _pool.Dequeue();
            obj.gameObject.SetActive(true);
            return obj;
        }
        
        var newObj = Object.Instantiate(_prefab, _container);
        return newObj;
    }
    
    public void Return(T obj)
    {
        obj.gameObject.SetActive(false);
        _pool.Enqueue(obj);
    }
}
```

### 7.2 垃圾回收优化 🟡

#### 7.2.1 避免GC分配

```csharp
// 正确示例：使用静态缓存
private static readonly Dictionary<string, AnimationClip> _animationCache = new Dictionary<string, AnimationClip>();

public AnimationClip GetAnimation(string name)
{
    if (_animationCache.TryGetValue(name, out var clip))
    {
        return clip;
    }
    
    clip = Resources.Load<AnimationClip>($"Animations/{name}");
    _animationCache[name] = clip;
    return clip;
}

// 使用垃圾回收友好的API
void Update()
{
    // 正确：使用CachedProperty
    transform.position = newPosition;
    
    // 错误：在Update中分配
    var position = new Vector3(transform.position.x, transform.position.y, 0);
}
```

#### 7.2.2 及时清理

```csharp
public void Cleanup()
{
    // 清理事件订阅
    EventManager.OnPlayerDied -= HandlePlayerDied;
    
    // 清理委托
    callback = null;
    
    // 清理集合
    _cachedData.Clear();
    _cachedData = null;
}
```

### 7.3 循环优化 🟡

```csharp
// 正确示例：缓存集合长度
for (int i = 0; i < _enemies.Count; i++)  // 缓存Count
{
    var enemy = _enemies[i];
    enemy.Update();
}

// 正确示例：使用for而不是foreach（热点代码）
for (int i = 0; i < 10000; i++)  // 性能敏感循环
{
    // 处理逻辑
}

// 使用合适的数据结构
// List: 随机访问
// HashSet: 成员检测
// Dictionary: 键值查找
```

### 7.4 缓存优化 🟡

```csharp
// 正确示例：缓存频繁访问的属性
private void Update()
{
    // 错误：每次都访问transform
    transform.position += Vector3.right * speed * Time.deltaTime;
    
    // 正确：缓存transform
    Vector3 right = Vector3.right;
    transform.position += right * _speed * Time.deltaTime;
}

// 缓存组件引用
private SpriteRenderer _spriteRenderer;
private Rigidbody2D _rigidbody;

private void Awake()
{
    _spriteRenderer = GetComponent<SpriteRenderer>();
    _rigidbody = GetComponent<Rigidbody2D>();
}
```

### 7.5 物理优化 🟡

```csharp
// 正确示例：使用物理层减少检测
LayerMask groundMask = LayerMask.GetMask("Ground", "Platform");

public bool IsGrounded()
{
    return Physics2D.OverlapCircle(_groundCheck.position, _checkRadius, groundMask) != null;
}

// 使用FixedUpdate进行物理计算
private void FixedUpdate()
{
    // 物理相关的代码
}

// 合理设置物理迭代次数
// Project Settings → Time → Fixed Timestep
```

---

## 8. Unity特定规范

### 8.1 MonoBehaviour生命周期 🟡

#### 8.1.1 生命周期方法顺序

```csharp
public class Example : MonoBehaviour
{
    // 1. 编辑器脚本重置
    private void Reset()
    {
        // 设置默认值
    }
    
    // 2. 唤醒（对象创建时）
    private void Awake()
    {
        // 单例初始化
        // 组件引用获取
        // 必要数据初始化
    }
    
    // 3. 启用（对象激活时）
    private void OnEnable()
    {
        // 事件订阅
    }
    
    // 4. 启动（第一帧前）
    private void Start()
    {
        // 初始化需要等待Awake完成的操作
    }
    
    // 5. 固定更新（物理更新）
    private void FixedUpdate()
    {
        // 物理相关计算
    }
    
    // 6. 更新（每帧）
    private void Update()
    {
        // 游戏逻辑
    }
    
    // 7. 晚更新（所有Update后）
    private void LateUpdate()
    {
        // 相机跟随
        // 同步操作
    }
    
    // 8. 禁用（对象禁用时）
    private void OnDisable()
    {
        // 事件取消订阅
    }
    
    // 9. 销毁（对象销毁时）
    private void OnDestroy()
    {
        // 资源清理
        // 静态事件取消订阅
    }
}
```

#### 8.1.2 生命周期方法使用规则

**规则：**
- `Awake`：单例初始化、组件引用获取
- `Start`：需要等待Awake完成的初始化
- `Update`：游戏逻辑更新
- `FixedUpdate`：物理计算
- `LateUpdate`：相机跟随、最终同步
- `OnDestroy`：清理工作

### 8.2 序列化规范 🔴

#### 8.2.1 SerializeField使用

```csharp
// 正确示例：私有字段序列化
[SerializeField] private int _maxHealth = 100;
[SerializeField] private float _moveSpeed = 5f;
[SerializeField] private GameObject _prefab;

// 保护序列化字段（编辑器代码可访问）
[SerializeField] protected int _health;

// 使用Header分组
[Header("生命值设置")]
[SerializeField] private int _maxHealth = 100;
[SerializeField] private int _currentHealth;

[Header("移动设置")]
[SerializeField] private float _moveSpeed = 5f;
[SerializeField] private float _runSpeed = 8f;

// 使用Tooltip提供说明
[Tooltip("最大生命值，范围1-999")]
[SerializeField] private int _maxHealth = 100;

// 使用Range限制范围
[Range(0, 100)]
[SerializeField] private int _health = 100;
```

#### 8.2.2 public vs SerializeField

**规则：**
- 公共字段：用于需要在Inspector中编辑且对其他脚本可见的字段
- [SerializeField]：用于需要在Inspector中编辑但对其他脚本不可见的字段
- private字段：用于内部状态，不在Inspector中显示

```csharp
// 正确示例
public int MaxHealth = 100;  // 公开配置，可在Inspector编辑

[SerializeField] private int _health;  // 私有状态，但可在Inspector编辑

private bool _isDead;  // 私有状态，不在Inspector显示

public int CurrentHealth => _health;  // 通过属性访问
```

### 8.3 组件引用 🟡

#### 8.3.1 获取组件

```csharp
// 正确示例：Awake中获取组件
public class Player : MonoBehaviour
{
    private SpriteRenderer _spriteRenderer;
    private Rigidbody2D _rigidbody;
    private BoxCollider2D _collider;
    
    private void Awake()
    {
        _spriteRenderer = GetComponent<SpriteRenderer>();
        _rigidbody = GetComponent<Rigidbody2D>();
        _collider = GetComponent<BoxCollider2D>();
        
        // 检查组件是否存在
        if (_spriteRenderer == null)
        {
            Debug.LogError("Player需要SpriteRenderer组件");
        }
    }
}

// 使用泛型版本（C#）
private void Awake()
{
    _spriteRenderer = GetComponent<SpriteRenderer>();
    _rigidbody = GetComponent<Rigidbody2D>();
}

// 获取子对象组件
private void Awake()
{
    _healthBar = GetComponentInChildren<HealthBar>();
    _hitBox = GetComponentsInChildren<HitBox>();
}
```

#### 8.3.2 RequireComponent

```csharp
// 使用RequireComponent确保依赖组件存在
[RequireComponent(typeof(Rigidbody2D))]
[RequireComponent(typeof(Collider2D))]
public class PlayerController : MonoBehaviour
{
    private Rigidbody2D _rigidbody;
    private Collider2D _collider;
    
    private void Awake()
    {
        _rigidbody = GetComponent<Rigidbody2D>();
        _collider = GetComponent<Collider2D>();
    }
}
```

### 8.4 Coroutine使用规范 🟡

```csharp
// 正确示例：使用协程
public IEnumerator LoadSceneAsync(string sceneName)
{
    // 显示加载界面
    _loadingUI.SetActive(true);
    
    // 开始加载
    AsyncOperation operation = SceneManager.LoadSceneAsync(sceneName);
    
    // 等待加载完成
    while (!operation.isDone)
    {
        float progress = operation.progress;
        _loadingText.text = $"加载中... {(progress * 100):F0}%";
        yield return null;
    }
    
    // 隐藏加载界面
    _loadingUI.SetActive(false);
}

// 启动协程
private void Start()
{
    StartCoroutine(LoadSceneAsync("GameScene"));
}

// 停止协程
private void OnDisable()
{
    StopAllCoroutines();
    StopCoroutine(_loadingCoroutine);
}

// 使用DOTween等插件替代复杂协程逻辑
```

### 8.5 事件系统 🟡

#### 8.5.1 Unity事件

```csharp
// 使用Unity事件（C#）
public UnityEvent OnGameStart;
public UnityEvent<int> OnScoreChanged;

// 使用UnityAction
public void SubscribeToEvents()
{
    EventManager.OnPlayerDied += HandlePlayerDied;
}

public void UnsubscribeFromEvents()
{
    EventManager.OnPlayerDied -= HandlePlayerDied;
}

private void OnDestroy()
{
    UnsubscribeFromEvents();
}
```

#### 8.5.2 自定义事件系统

```csharp
// 事件参数类
public class PlayerEventArgs : EventArgs
{
    public Player Player { get; }
    public int Damage { get; }
    
    public PlayerEventArgs(Player player, int damage)
    {
        Player = player;
        Damage = damage;
    }
}

// 事件声明
public static class EventManager
{
    public static event EventHandler<PlayerEventArgs> OnPlayerDied;
    public static event Action<int> OnScoreChanged;
    
    public static void PlayerDied(Player player, int damage)
    {
        OnPlayerDied?.Invoke(null, new PlayerEventArgs(player, damage));
    }
    
    public static void ScoreChanged(int score)
    {
        OnScoreChanged?.Invoke(score);
    }
}
```

### 8.6 资源管理 🟡

#### 8.6.1 Resources加载

```csharp
// 正确示例：Resources加载
private Sprite LoadIcon(string iconName)
{
    return Resources.Load<Sprite>($"Icons/{iconName}");
}

private void LoadAllIcons()
{
    var icons = Resources.LoadAll<Sprite>("Icons");
    foreach (var icon in icons)
    {
        _iconCache[icon.name] = icon;
    }
}

// 错误示例：在Update中加载Resources
private void Update()
{
    if (Input.GetKeyDown(KeyCode.Space))
    {
        var sprite = Resources.Load<Sprite>("HugeTexture");  // 性能问题
    }
}
```

#### 8.6.2 Addressables使用

```csharp
// 使用Addressables进行资源管理
public async void LoadPlayerPrefabAsync()
{
    var handle = Addressables.LoadAssetAsync<GameObject>("PlayerPrefab");
    await handle.Task;
    var playerPrefab = handle.Result;
    
    // 使用后释放
    Addressables.Release(handle);
}
```

---

## 9. 设计模式应用

### 9.1 单例模式 🟡

```csharp
// 方案A：基础单例
public class GameManager : MonoBehaviour
{
    public static GameManager Instance { get; private set; }
    
    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}

// 方案B：泛型单例基类
public abstract class Singleton<T> : MonoBehaviour where T : MonoBehaviour
{
    public static T Instance { get; private set; }
    
    protected virtual void Awake()
    {
        if (Instance == null)
        {
            Instance = this as T;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}

// 使用
public class AudioManager : Singleton<AudioManager>
{
    protected override void Awake()
    {
        base.Awake();
        // AudioManager特定的初始化
    }
}
```

### 9.2 工厂模式 🟡

```csharp
// 简单工厂
public class ItemFactory
{
    public Item CreateItem(ItemType type)
    {
        switch (type)
        {
            case ItemType.HealthPotion:
                return new HealthPotion();
            case ItemType.ManaPotion:
                return new ManaPotion();
            case ItemType.Key:
                return new KeyItem();
            default:
                throw new ArgumentException($"未知的物品类型: {type}");
        }
    }
}

// 抽象工厂
public interface IEnemyFactory
{
    Enemy CreateEnemy(EnemyType type);
}

public class ZombieFactory : IEnemyFactory
{
    public Enemy CreateEnemy(EnemyType type)
    {
        // 创建僵尸
    }
}
```

### 9.3 观察者模式 🟡

```csharp
// 观察者接口
public interface IObserver<T>
{
    void OnNotify(T data);
}

// 主题
public class Subject<T>
{
    private List<IObserver<T>> _observers = new List<IObserver<T>>();
    
    public void Subscribe(IObserver<T> observer)
    {
        _observers.Add(observer);
    }
    
    public void Unsubscribe(IObserver<T> observer)
    {
        _observers.Remove(observer);
    }
    
    protected void Notify(T data)
    {
        foreach (var observer in _observers)
        {
            observer.OnNotify(data);
        }
    }
}
```

### 9.4 对象池模式 🟡

```csharp
// 通用对象池
public class ObjectPool<T> where T : Component
{
    private Queue<T> _pool = new Queue<T>();
    private T _prefab;
    private int _initialSize;
    
    public ObjectPool(T prefab, int initialSize = 10)
    {
        _prefab = prefab;
        _initialSize = initialSize;
        WarmUp();
    }
    
    public void WarmUp()
    {
        for (int i = 0; i < _initialSize; i++)
        {
            var obj = Object.Instantiate(_prefab);
            obj.gameObject.SetActive(false);
            _pool.Enqueue(obj);
        }
    }
    
    public T Get()
    {
        if (_pool.Count > 0)
        {
            var obj = _pool.Dequeue();
            obj.gameObject.SetActive(true);
            return obj;
        }
        
        return Object.Instantiate(_prefab);
    }
    
    public void Return(T obj)
    {
        obj.gameObject.SetActive(false);
        _pool.Enqueue(obj);
    }
}
```

### 9.5 状态模式 🟡

```csharp
// 状态接口
public interface IState
{
    void Enter();
    void Update();
    void Exit();
}

// 状态基类
public abstract class State : IState
{
    protected readonly StateMachine _stateMachine;
    
    protected State(StateMachine stateMachine)
    {
        _stateMachine = stateMachine;
    }
    
    public virtual void Enter() { }
    public virtual void Update() { }
    public virtual void Exit() { }
}

// 状态机
public class StateMachine
{
    private IState _currentState;
    private readonly Dictionary<Type, IState> _states = new Dictionary<Type, IState>();
    
    public void AddState<T>(T state) where T : IState
    {
        _states[typeof(T)] = state;
    }
    
    public void ChangeState<T>() where T : IState
    {
        _currentState?.Exit();
        _currentState = _states[typeof(T)];
        _currentState?.Enter();
    }
    
    public void Update()
    {
        _currentState?.Update();
    }
}
```

---

## 10. Git工作流规范

### 10.1 分支策略 🔴

#### 10.1.1 分支命名规范

| 分支类型 | 命名模式 | 说明 |
|---------|---------|------|
| 主分支 | `main` | 稳定版本，只接受发布或热修复 |
| 开发分支 | `develop` | 开发主分支，所有功能合并到这里 |
| 功能分支 | `feature/*` | 功能开发分支，从develop创建 |
| 发布分支 | `release/*` | 发布准备分支，从develop创建 |
| 热修复分支 | `hotfix/*` | 紧急修复分支，从main创建 |
| 实验分支 | `experiment/*` | 实验性开发，不参与正式流程 |

```bash
# 创建功能分支
git checkout develop
git checkout -b feature/player-combat-system

# 创建热修复分支
git checkout main
git hotfix/critical-bug-fix

# 创建发布分支
git checkout develop
git checkout -b release/v1.0.0
```

#### 10.1.2 分支生命周期

```
main:     ─●──────────────────●──────────────────●──
              │                │                │
             hotfix          merge           merge
              │                │                │
              └●──────────────┘                │
                │                             │
develop:  ─●────●────────●────────●──────●────●──
               │        │        │      │    │
          feature/   feature/  ...   merge merge
          player     combat
```

### 10.2 提交规范 🔴

#### 10.2.1 提交信息格式

**格式：** `<type>: <subject>`

```
feat: 添加玩家移动和跳跃功能
fix: 修复装备栏的物品堆叠问题
docs: 更新README中的安装说明
style: 调整代码格式符合规范
refactor: 重构事件系统，改为泛型实现
perf: 优化对象池的初始化逻辑
test: 添加玩家控制器的单元测试
chore: 更新Git忽略文件
ci: 配置CI/CD构建流程
```

#### 10.2.2 Type类型

| 类型 | 说明 |
|-----|------|
| **feat** | 新功能 |
| **fix** | Bug修复 |
| **docs** | 文档更新 |
| **style** | 代码格式调整（不影响功能） |
| **refactor** | 重构（既不是新功能也不是修复bug） |
| **perf** | 性能优化 |
| **test** | 测试相关 |
| **chore** | 构建/工具相关 |
| **ci** | CI/CD相关 |
| **build** | 构建系统相关 |
| **revert** | 回滚提交 |

#### 10.2.3 提交信息模板

```
<类型>(<范围>): <主题>

<body>

<footer>
```

```bash
feat(玩家系统): 添加玩家移动控制

实现玩家角色的基础移动功能，包括：
- 键盘输入响应
- 移动速度控制
- 移动方向限制

Fixes #123
Closes #456
```

### 10.3 合并策略 🔴

#### 10.3.1 合并流程

```bash
# 1. 确保工作目录干净
git status
git stash

# 2. 更新develop分支
git checkout develop
git pull origin develop

# 3. 切换到功能分支
git checkout feature/xxx

# 4. 变基到最新develop
git rebase develop

# 5. 解决冲突（如果有）
git add .
git rebase --continue

# 6. 推送到远程
git push origin feature/xxx --force

# 7. 创建Pull Request
```

#### 10.3.2 合并方式

**优先使用变基（Rebase）**

```bash
# 变基到目标分支
git checkout feature/xxx
git rebase develop

# 交互式变基（修改提交历史）
git rebase -i develop
```

**保留历史清晰的合并提交**

```bash
# 使用--no-ff保留功能分支的完整历史
git checkout develop
git merge --no-ff feature/xxx
```

### 10.4 标签管理 🟡

#### 10.4.1 标签命名

```bash
# 版本标签
v1.0.0
v1.0.1
v2.0.0

# 预发布标签
v1.0.0-alpha
v1.0.0-beta.1
v1.0.0-rc.1

# 功能标签
feature/player-combat
feature/new-ai-system
```

#### 10.4.2 创建标签

```bash
# 创建附注标签
git tag -a v1.0.0 -m "Version 1.0.0"

# 创建轻量标签
git tag v1.0.0

# 推送标签
git push origin v1.0.0

# 推送所有标签
git push origin --tags
```

---

## 11. 代码审查规范

### 11.1 审查流程 🔴

#### 11.1.1 Pull Request要求

**创建PR前检查清单：**
- [ ] 代码符合编码规范
- [ ] 所有测试通过
- [ ] 文档已更新
- [ ] 无编译错误
- [ ] 分支已更新到最新
- [ ] 已进行本地测试

```markdown
## PR描述模板

### 描述
[简要描述这次变更]

### 变更类型
- [ ] 新功能
- [ ] Bug修复
- [ ] 重构
- [ ] 文档更新
- [ ] 其他

### 测试
- [ ] 已添加单元测试
- [ ] 已进行手动测试
- [ ] 测试覆盖所有更改

### 检查清单
- [ ] 代码符合编码规范
- [ ] 无编译警告
- [ ] 分支已同步最新develop
```

#### 11.1.2 审查者职责

```markdown
## 代码审查清单

### 代码质量
- [ ] 代码清晰易懂
- [ ] 命名规范一致
- [ ] 方法职责单一
- [ ] 没有重复代码

### 功能正确性
- [ ] 逻辑正确
- [ ] 边界情况处理
- [ ] 错误处理完善
- [ ] 没有明显的Bug

### 性能
- [ ] 没有明显的性能问题
- [ ] 资源使用合理
- [ ] 避免不必要的分配

### 兼容性
- [ ] 考虑向后兼容
- [ ] API设计合理
```

### 11.2 审查标准 🟡

#### 11.2.1 必须修复的问题

```markdown
## 🔴 必须修复（P0）

- 编译错误
- 运行时崩溃
- 逻辑错误
- 安全漏洞
- 违反编码规范
```

#### 11.2.2 建议修改的问题

```markdown
## 🟡 建议修改（P1）

- 代码可读性问题
- 性能优化建议
- 更好的命名建议
- 缺少注释或文档
- 潜在的问题（未验证）
```

#### 11.2.3 可以讨论的问题

```markdown
## 🟢 可以讨论（P2）

- 架构选择
- 设计模式应用
- 第三方库使用
- 代码风格偏好
```

### 11.3 审查反馈规范 🟡

```markdown
## 反馈格式

### 问题描述
[描述问题]

### 影响
[说明问题的影响]

### 建议
[提出改进建议]

### 示例
```csharp
// 当前代码
var result = GetData();

// 建议修改
var data = GetData();
var result = ProcessData(data);
```
```

---

## 12. 测试规范

### 12.1 测试分类 🟡

| 测试类型 | 说明 | 工具 |
|---------|------|------|
| **单元测试** | 测试单个方法/类 | NUnit, Unity Test Framework |
| **集成测试** | 测试模块间交互 | NUnit, Unity Test Framework |
| **端到端测试** | 测试完整功能流程 | Unity Automation |
| **性能测试** | 测试性能指标 | Unity Profiler |

### 12.2 单元测试规范 🔴

#### 12.2.1 测试命名

```csharp
[Test]
public void CalculateDamage_WithPositiveValues_ReturnsCorrectDamage()
{
    // Arrange
    var calculator = new DamageCalculator();
    
    // Act
    var damage = calculator.CalculateDamage(attack: 100, defense: 50);
    
    // Assert
    Assert.AreEqual(50, damage);
}

[Test]
public void CalculateDamage_WithZeroAttack_ReturnsZeroDamage()
{
    // Arrange
    var calculator = new DamageCalculator();
    
    // Act
    var damage = calculator.CalculateDamage(attack: 0, defense: 50);
    
    // Assert
    Assert.AreEqual(0, damage);
}

[Test]
public void CalculateDamage_WithNegativeDefense_ThrowsException()
{
    // Arrange
    var calculator = new DamageCalculator();
    
    // Act & Assert
    Assert.Throws<ArgumentException>(() => 
        calculator.CalculateDamage(attack: 100, defense: -10));
}
```

#### 12.2.2 测试结构

```csharp
[TestFixture]
public class DamageCalculatorTests
{
    private DamageCalculator _calculator;
    
    [SetUp]
    public void SetUp()
    {
        _calculator = new DamageCalculator();
    }
    
    [TearDown]
    public void TearDown()
    {
        _calculator = null;
    }
    
    [Test]
    public void CalculateDamage_ValidInput_ReturnsCorrectResult()
    {
        // 测试代码
    }
}
```

### 12.3 Unity测试 🟡

```csharp
using UnityEngine;
using UnityEngine.TestTools;
using NUnit.Framework;

public class PlayerTests
{
    [Test]
    public void Player_StartsWithMaxHealth()
    {
        // Arrange
        var player = new GameObject("Player").AddComponent<Player>();
        
        // Assert
        Assert.AreEqual(player.MaxHealth, player.CurrentHealth);
    }
    
    [UnityTest]
    public IEnumerator Player_TakesDamage_HealthDecreases()
    {
        // Arrange
        var player = new GameObject("Player").AddComponent<Player>();
        int initialHealth = player.CurrentHealth;
        
        // Act
        player.TakeDamage(10);
        
        // Assert
        Assert.AreEqual(initialHealth - 10, player.CurrentHealth);
        
        yield return null;
    }
}
```

---

## 13. 文档规范

### 13.1 代码文档 🟡

#### 13.1.1 必写文档

```csharp
/// <summary>
/// 玩家控制类，负责处理玩家的移动、攻击和交互
/// </summary>
public class PlayerController : MonoBehaviour
{
    /// <summary>
    /// 最大生命值
    /// </summary>
    [SerializeField] private int _maxHealth = 100;
    
    /// <summary>
    /// 移动速度（单位/秒）
    /// </summary>
    [SerializeField] private float _moveSpeed = 5f;
    
    /// <summary>
    /// 初始化玩家控制器
    /// </summary>
    /// <param name="spawnPoint">出生点位置</param>
    /// <returns>初始化是否成功</returns>
    public bool Initialize(Vector3 spawnPoint)
    {
        // 实现
    }
    
    /// <summary>
    /// 造成伤害
    /// </summary>
    /// <param name="damage">伤害值</param>
    /// <exception cref="ArgumentException">伤害值小于0时抛出</exception>
    public void TakeDamage(int damage)
    {
        // 实现
    }
}
```

### 13.2 项目文档 🟡

#### 13.2.1 必需文档

| 文档 | 说明 | 位置 |
|-----|------|------|
| README.md | 项目说明 | 项目根目录 |
| CONTRIBUTING.md | 贡献指南 | 项目根目录 |
| CHANGELOG.md | 更新日志 | 项目根目录 |
| API文档 | 接口说明 | Docs/ |
| 架构文档 | 系统架构 | Docs/Architecture/ |
| 设计文档 | 游戏设计 | Docs/Design/ |

#### 13.2.2 文档格式

```markdown
# 项目名称

## 概述
[项目简介]

## 功能特性
- 特性1
- 特性2
- 特性3

## 环境要求
- Unity版本：2022.3+
- .NET版本：.NET 4.x

## 安装说明
1. 克隆仓库
2. 打开Unity项目
3. 导入必要的包

## 使用说明
[使用方法]

## 贡献指南
见CONTRIBUTING.md

## 更新日志
见CHANGELOG.md

## 许可证
MIT License
```

---

## 14. 最佳实践

### 14.1 SOLID原则 🟡

#### 14.1.1 单一职责原则

```csharp
// 正确示例：每个类一个职责
public class PlayerHealth : MonoBehaviour
{
    [SerializeField] private int _maxHealth;
    private int _currentHealth;
    
    public void TakeDamage(int damage) { }
    public void Heal(int amount) { }
    public bool IsAlive => _currentHealth > 0;
}

public class PlayerMovement : MonoBehaviour
{
    public void Move(Vector2 direction) { }
    public void Jump() { }
    public void Dash() { }
}

// 错误示例：一个类多个职责
public class Player : MonoBehaviour
{
    // 生命值管理
    public void TakeDamage(int damage) { }
    public void Heal(int amount) { }
    
    // 移动控制
    public void Move(Vector2 direction) { }
    public void Jump() { }
    
    // 战斗系统
    public void Attack() { }
    public void UseSkill(int skillId) { }
    
    // 存档系统
    public void SavePlayerData() { }
    public void LoadPlayerData() { }
}
```

#### 14.1.2 开放封闭原则

```csharp
// 正确示例：对扩展开放，对修改封闭
public abstract class Damageable : MonoBehaviour
{
    public abstract void TakeDamage(int damage);
}

public class Enemy : Damageable
{
    public override void TakeDamage(int damage)
    {
        // 敌人受伤逻辑
    }
}

public class Player : Damageable
{
    public override void TakeDamage(int damage)
    {
        // 玩家受伤逻辑
    }
}

// 新增受伤类型不需要修改Damageable
public class Boss : Damageable
{
    public override void TakeDamage(int damage)
    {
        // Boss受伤逻辑
    }
}
```

#### 14.1.3 依赖倒置原则

```csharp
// 正确示例：依赖抽象
public interface IPlayerRepository
{
    PlayerData Load(string savePath);
    void Save(string savePath, PlayerData data);
}

public class PlayerService
{
    private readonly IPlayerRepository _repository;
    
    public PlayerService(IPlayerRepository repository)
    {
        _repository = repository;
    }
    
    public PlayerData LoadPlayer(string savePath)
    {
        return _repository.Load(savePath);
    }
}

// 使用
var repository = new JsonPlayerRepository();
var service = new PlayerService(repository);
```

### 14.2 DRY原则 🟡

```csharp
// 正确示例：提取公共逻辑
public static class MathHelper
{
    public static float Clamp(float value, float min, float max)
    {
        return Mathf.Max(min, Mathf.Min(max, value));
    }
    
    public static int Clamp(int value, int min, int max)
    {
        return Math.Max(min, Math.Min(max, value));
    }
}

// 错误示例：重复代码
public void UpdatePlayer1Health(int health)
{
    health = Mathf.Max(0, Mathf.Min(100, health));
    _player1.Health = health;
}

public void UpdatePlayer2Health(int health)
{
    health = Mathf.Max(0, Mathf.Min(100, health));  // 重复
    _player2.Health = health;
}
```

### 14.3 KISS原则 🟡

```csharp
// 正确示例：简单直接
public bool IsDead => _currentHealth <= 0;

// 错误示例：过度复杂
public bool IsDead
{
    get
    {
        if (_currentHealth == null)
        {
            return false;
        }
        
        if (_currentHealth.Value <= 0)
        {
            return true;
        }
        else
        {
            return false;
        }
    }
}
```

---

## 15. 常见问题解答

### Q1: 什么时候应该使用`static`方法？

**A:** 
- 方法不依赖于实例状态
- 方法是工具函数（如MathHelper）
- 需要避免创建对象时（如单例的静态方法）

### Q2: 什么时候应该使用`struct`而不是`class`？

**A:**
- 数据量小（通常<16字节）
- 需要值语义（比较时用值比较）
- 不需要继承

```csharp
// 适合用struct的例子
public struct Vector2Int
{
    public int x;
    public int y;
}

// 不适合用struct的例子
public struct PlayerData
{
    public string Name;
    public int Health;
    public List<InventoryItem> Inventory;  // 包含引用类型
}
```

### Q3: 什么时候应该使用`interface`？

**A:**
- 需要定义契约
- 需要支持多实现
- 需要解耦具体实现

### Q4: 什么是"魔法数字"？如何处理？

**A:** "魔法数字"是代码中直接出现的字面值，缺乏解释。

```csharp
// 错误示例：魔法数字
if (player.Health < 10)  // 10是什么意思？
{
    ShowLowHealthWarning();
}

// 正确示例：使用常量或配置
private const int LOW_HEALTH_THRESHOLD = 10;

if (player.Health < LOW_HEALTH_THRESHOLD)
{
    ShowLowHealthWarning();
}
```

### Q5: 如何处理长方法？

**A:** 
1. 提取子方法
2. 使用guard clause简化条件
3. 使用策略模式分解逻辑

```csharp
// 原始长方法
public void ProcessPlayerInput()
{
    if (Input.GetKeyDown(KeyCode.Space))
    {
        // 跳跃逻辑（50行）
    }
    
    if (Input.GetKey(KeyCode.LeftShift))
    {
        // 奔跑逻辑（30行）
    }
    
    // 更多逻辑...
}

// 重构后
public void ProcessPlayerInput()
{
    HandleJumpInput();
    HandleRunInput();
    HandleMoveInput();
    HandleAttackInput();
}

private void HandleJumpInput()
{
    if (Input.GetKeyDown(KeyCode.Space))
    {
        Jump();
    }
}
```

---

## 16. 附录

### 16.1 常用缩写表 🟡

| 全称 | 缩写 | 使用建议 |
|-----|------|---------|
| Manager | Mgr | 不推荐使用缩写 |
| Controller | Ctrl | 在UI相关中使用 |
| Configuration | Config | 可接受 |
| Initialize | Init | 可接受 |
| Maximum | Max | 可接受 |
| Minimum | Min | 可接受 |
| Temporary | Temp | 可接受 |
| Number | Num | 在循环中使用 |
| String | Str | 不推荐 |
| Boolean | Bool | 可接受 |
| Integer | Int | 可接受 |
| Character | Char | 可接受 |

### 16.2 检查清单 🟢

#### 创建新脚本前检查

- [ ] 是否符合单一职责原则
- [ ] 是否需要使用设计模式
- [ ] 是否需要创建接口
- [ ] 命名是否规范
- [ ] 是否需要文档注释

#### 提交代码前检查

- [ ] 代码符合规范
- [ ] 没有未使用的using语句
- [ ] 没有编译警告
- [ ] 单元测试通过
- [ ] 分支已同步最新
- [ ] 提交信息符合规范

#### 创建PR前检查

- [ ] 所有检查通过
- [ ] 文档已更新
- [ ] 已进行手动测试
- [ ] 没有敏感信息泄露
- [ ] 分支命名规范

### 16.3 参考资源 📚

- [C#编码约定](https://docs.microsoft.com/zh-cn/dotnet/csharp/programming-guide/inside-a-program/coding-conventions)
- [Unity编程最佳实践](https://unity3d.com/learn/tutorials/topics/best-practices)
- [微软.NET命名指南](https://docs.microsoft.com/zh-cn/dotnet/standard/design-guidelines/naming-guidelines)
- [Pro Git书籍](https://git-scm.com/book/zh/v2)

---

**文档版本：** 1.0.0  
**最后更新：** 2026-02-03  
**维护者：** 项目开发团队

本规范将随项目进展持续更新，如有疑问或建议，请提交Issue或联系维护团队。
